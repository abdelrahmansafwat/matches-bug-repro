{
  "openapi": "3.0.0",
  "paths": {
    "/api/v1/demo": {
      "get": {
        "operationId": "getHello",
        "parameters": [],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": { "$ref": "#/components/schemas/DemoDto" }
                }
              }
            }
          }
        },
        "tags": ["Demo"]
      },
      "post": {
        "operationId": "createHello",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": { "$ref": "#/components/schemas/CreateDemoDto" }
            }
          }
        },
        "responses": {
          "201": {
            "description": "",
            "content": {
              "application/json": {
                "schema": { "$ref": "#/components/schemas/DemoDto" }
              }
            }
          }
        },
        "tags": ["Demo"]
      }
    }
  },
  "info": {
    "title": "Matches Bug Reproduction",
    "description": "Demo API to reproduce the bug caused by using matches in API validation",
    "version": "1.0.0",
    "contact": {}
  },
  "tags": [],
  "servers": [{ "url": "http://localhost:3000" }],
  "components": {
    "securitySchemes": {
      "bearer": { "scheme": "bearer", "bearerFormat": "JWT", "type": "http" }
    },
    "schemas": {
      "DemoDto": {
        "type": "object",
        "properties": {
          "name": { "type": "string" },
          "age": { "type": "number" }
        },
        "required": ["name"]
      },
      "CreateDemoDto": {
        "type": "object",
        "properties": { "age": { "type": "number" } }
      }
    }
  }
}
